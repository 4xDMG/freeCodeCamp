function repeat(str, num) {
  // repeat after me
  var strA = str;
  
  if (num > 0) {
    for (i = 1; i < num; i++) {
      str = str + strA;
    }
  } else {
    str = '';
  }
  
  return str;
}

repeat("abc", -2);
